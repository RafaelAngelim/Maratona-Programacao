#include <stdio.h>

int main(){
    
    int fri, bottle, lbo, limes, slices, salt, ml, mp, numbsl;
    scanf("%d %d %d %d %d %d %d %d", &fri, &bottle, &lbo, &limes, &slices, &salt, &ml, &mp);

    numbsl = limes * slices;
    
    

}
